#include <stdio.h>

void main(){
    int num;
    scanf("%d", &num);

    void nada(int num);
        int produto = 1;
        int soma = 1;

        while (num !=9){
            if (num % 2 == 0){
                int Produto = produto * num;
            } else {
                soma = soma + num;
            }
        nada(num);
    }
    printf("%d, %d", produto, soma);
}



